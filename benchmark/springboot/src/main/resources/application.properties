# Spring Boot benchmark server configuration.
server.port=8090

# Use Java virtual threads (Project Loom) per request -- the closest analogue to
# SwiftNet's virtual-thread model.
spring.threads.virtual.enabled=true

# Keep logging out of the hot path during benchmarks.
logging.level.root=WARN
spring.main.banner-mode=off
